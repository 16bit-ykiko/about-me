name: test libcxx link

on: workflow_dispatch

jobs:
  test:
    runs-on: macos-15
    steps:
      - name: Setup pixi
        uses: prefix-dev/setup-pixi@v0.8.1
        with:
          run-install: false

      - name: Create env
        run: |
          cat > pixi.toml << 'TOML'
          [workspace]
          name = "test"
          channels = ["conda-forge"]
          platforms = ["osx-arm64"]
          [dependencies]
          clang = "==22.1.8"
          clangxx = "==22.1.8"
          lld = "==22.1.8"
          libcxx = "==22.1.8"
          libcxx-devel = "==22.1.8"
          TOML
          pixi install

      - name: Write test
        run: |
          cat > test.cpp << 'CPP'
          #include <string>
          #include <unordered_map>
          int main() {
              std::unordered_map<std::string, int> m;
              m["hello"] = 1;
              return m.size() == 1 ? 0 : 2;
          }
          CPP

      - name: Baseline native link (control, expect @rpath)
        run: |
          pixi run clang++ -std=c++23 -fuse-ld=lld test.cpp -o base
          otool -L base
          otool -l base | grep -A2 LC_RPATH || echo "(no rpath)"
          otool -L base | grep -q '@rpath/libc++' || { echo "control failed: expected @rpath"; exit 1; }

      - name: A sdk libdir prepended
        run: |
          SDK=$(xcrun --show-sdk-path)
          pixi run clang++ -std=c++23 -fuse-ld=lld "-L$SDK/usr/lib" test.cpp -o cand_a
          otool -L cand_a
          otool -l cand_a | grep -A2 LC_RPATH || echo "(no rpath)"
          otool -L cand_a | grep -q '/usr/lib/libc++.1.dylib' || { echo "A failed"; exit 1; }
          ./cand_a; echo "exit=$?"

      - name: B nostdlib++ plus explicit sdk tbd
        run: |
          SDK=$(xcrun --show-sdk-path)
          pixi run clang++ -std=c++23 -fuse-ld=lld -nostdlib++ \
            test.cpp "$SDK/usr/lib/libc++.tbd" -o cand_b
          otool -L cand_b
          otool -l cand_b | grep -A2 LC_RPATH || echo "(no rpath)"
          otool -L cand_b | grep -q '/usr/lib/libc++.1.dylib' || { echo "B failed"; exit 1; }
          ./cand_b; echo "exit=$?"

      - name: A on x64 cross stays clean
        run: |
          SDK=$(xcrun --show-sdk-path)
          pixi run clang++ -std=c++23 -fuse-ld=lld -target x86_64-apple-darwin \
            "-L$SDK/usr/lib" test.cpp -o cand_a_x64
          otool -L cand_a_x64
          otool -L cand_a_x64 | grep -q '/usr/lib/libc++.1.dylib' || { echo "A-x64 failed"; exit 1; }

      - name: C no-default-config native
        run: |
          pixi run clang++ -std=c++23 -fuse-ld=lld --no-default-config test.cpp -o cand_c
          otool -L cand_c
          otool -l cand_c | grep -A2 LC_RPATH || echo "(no rpath)"
          otool -L cand_c | grep -q '/usr/lib/libc++.1.dylib' || { echo "C failed: not system libc++"; exit 1; }
          if otool -l cand_c | grep -q '.pixi/envs'; then echo "C failed: env rpath present"; exit 1; fi
          ./cand_c
          # headers must still be conda libc++ 22 (driver-relative search):
          pixi run clang++ -std=c++23 --no-default-config -c test.cpp -o cand_c.o
          nm cand_c.o | grep __hash_memory
          nm -u cand_c.o | grep __hash_memory && exit 1 || echo "inline fallback, no extern ref"

      - name: C no-default-config x64 cross
        run: |
          pixi run clang++ -std=c++23 -fuse-ld=lld -target x86_64-apple-darwin \
            --no-default-config test.cpp -o cand_c_x64
          otool -L cand_c_x64
          otool -L cand_c_x64 | grep -q '/usr/lib/libc++.1.dylib' || { echo "C-x64 failed"; exit 1; }
